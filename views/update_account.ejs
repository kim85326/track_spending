<!DOCTYPE html>
<html>
  <head>
    <title>小記帳</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  </head>
  <body>
     <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">小記帳</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <form class="navbar-form navbar-right">
            <button type="submit" class="btn btn-success">新增消費</button>
          </form>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>

    <div class="container main-container">
      <h2>更新消費</h2>
      <form action="/accounts/<%= account.id %>/update" method="post">
        <div class="form-group">
          <input class="form-control" type="text" name="title" placeholder="請輸入消費標題" value="<%= account.title %>">
        </div>
        <div class="form-group">
          <select class="form-control" name="type">
              <option value="">請選擇類別</option>
              <option <%=account.type==='eat' ? 'selected' : '' %>  value="eat">食</option>
              <option <%=account.type==='cloth' ? 'selected' : '' %> value="cloth">衣</option>
              <option <%=account.type==='home' ? 'selected' : '' %> value="home">住</option>
              <option <%=account.type==='traffic' ? 'selected' : '' %> value="traffic">行</option>
              <option <%=account.type==='play' ? 'selected' : '' %> value="play">育樂</option>
          </select>
        </div>
        <div class="form-group">
          <input class="form-control" type="number" name="cost" placeholder="請輸入消費金額" value="<%= account.cost %>">
        </div>
        <button class="btn btn-primary btn-block" type="submit">送出</button>
      </form>
       
      <hr>

      <footer>
        <p>&copy; 2017 Company, Inc.</p>
      </footer>
    </div> <!-- /container -->

  </body>
</html>
